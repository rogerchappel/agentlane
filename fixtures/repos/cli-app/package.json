{
  "name": "fixture-cli-app",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "smoke": "node dist/cli.js --help"
  }
}
